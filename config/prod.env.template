# ============================================================
# Environment Config Template : prod
# Copy to prod.env and fill in values.
# prod.env is gitignored — use Jenkins credentials instead.
# ============================================================

DB_HOST=<prod-db-host>
DB_PORT=5432
DB_USER=<db-user>
DB_PASSWORD=<db-password>
DB_NAME=chinook
