# ============================================================
# Environment Config Template : staging
# Copy to staging.env and fill in values.
# staging.env is gitignored — never commit real credentials.
# ============================================================

DB_HOST=<staging-db-host>
DB_PORT=5432
DB_USER=<db-user>
DB_PASSWORD=<db-password>
DB_NAME=chinook
